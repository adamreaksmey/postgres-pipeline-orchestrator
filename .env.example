DATABASE_URL=postgresql://admin:password@localhost:5432/cicd
# optional
PORT=3000
SWAGGER_PATH=docs
# If true, this process runs the worker loop
RUN_WORKER_LOOP=false
# Set true only in the API process; workers must use false to avoid DDL races (XX000 / tuple concurrently updated)
SYNC_DATABASE=true
